// import PropTypes from 'prop-types';

// export const selectedContact = PropTypes.oneOf([
// 	PropTypes.object,
// 	PropTypes.objectOf({
// 		general: PropTypes.object,
// 		contact: PropTypes.object,
// 		job: PropTypes.object,
// 		address: PropTypes.object
// 	})
// ]);

// export const contacts = PropTypes.arrayOf( [selectedContact] );

// export const fetchContacts = PropTypes.requiredFunc;

// export const selectContact = PropTypes.requiredFunc;

// export const deselectContact = PropTypes.requiredFunc;


